# perimeter-of-triangle
#include <stdio.h>

int main()
{
    int side, perimeter;
    printf("enter the value of side: ");
    scanf("%d", &side);
    printf("perimeter is %d", perimeter=3*side);
    return 0;
}
